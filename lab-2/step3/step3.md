# Objective 3

In your downloads folder, there is a zip file. This file was actually downloaded by a hacker who compromised the system.

Rename this file using mv to `suspicious_download.zip`, then move it to the `quarantine` directory.

It is best to quarantine suspicious files so it does not further damage the system. 

*NOTE : Quarantining files in real practice is not as simple as this.

Finally, copy the `critical_info.txt` to `critical_info.backup` using cp.