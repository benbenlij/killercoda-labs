# Objective 2

Now we will practice creating and removing our own files!

To protect the system, we will log the compromised services. Create a empty file using `touch` titled `compromised_services.log`.

It is also best to keep an organized filesystem. Remove the `unauthorized_image.jpg` image using rm.